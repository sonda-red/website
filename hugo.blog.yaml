baseURL: "https://blog.sonda.red/"
title: "blog.sonda.red"

module:
  mounts:
    - source: content/blog
      target: content
    - source: assets
      target: assets
    - source: layouts
      target: layouts
    - source: static
      target: static

params:
  description: "blog.sonda.red is the Sonda Red notebook and article archive."

  editURL:
    enable: true
    base: "https://github.com/sonda-red/blog/edit/main/content/blog"
